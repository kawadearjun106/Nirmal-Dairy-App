<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.nirmal.app">

    <!-- ✅ Permissions must be declared here, outside the <application> -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NirmalApp"
        tools:targetApi="31">

        <activity android:name=".AuthActivity" />
        <activity android:name=".AdminDashboardActivity" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddMilkEntryActivity" />
        <activity android:name=".ViewMilkEntriesActivity" />
        <activity android:name=".DailyReportActivity" />
        <activity android:name=".ManageFarmersActivity" />
        <activity android:name=".ManageProductsActivity" />
        <activity android:name=".ManagePricesActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".FarmerDashboardActivity" />
        <activity android:name=".CustomerDashboardActivity" />
        <activity android:name=".ViewProductsActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".BillActivity" />
        <activity android:name=".FarmerBillsActivity" />
        <activity android:name=".FarmerMonthlyReportActivity" />
        <activity android:name=".AdminViewEntriesActivity" />
        <activity android:name=".AdminFarmerEntriesActivity" />

    </application>
</manifest>
